# == supernode configuration values parsed by /etc/rc.d/init.d/edged ==
# Each value is read by the init script - do not modify variable names.
# Read 'man edge' for details about these values...

N2N_PORT="443";
N2N_ALLOW_COMMUNITY_FILE="community.list";
# Other options...
#   N2N_OPTIONS="-v"
N2N_OPTIONS="";
# === end supernode config. file ===================
